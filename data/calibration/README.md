# Calibration data

This directory is for local BenchBase calibration inputs used by the query-level simulator.

Large calibration CSV and JSON files are intentionally not committed.

Expected local input:

q1_query_observations.csv
